/**
 * 创建函数的方法
 * 1. 使用 `函数` 关键字
 * 2. lambda 表达式
 */
赋值 context = "456789";

函数 A(a,b,c) {
    打印换行(a,b,c)
}

函数 A(a) {
    打印换行(a)
}

赋值 B = (x) => {
    打印换行(x)
}

A("你好")

赋值 B = () => {
    打印换行("hello")
}
// 调用函数
A(1,2,3);
A(456);
B(context);
B();
