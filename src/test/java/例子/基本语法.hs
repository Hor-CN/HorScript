/**
* HorScript 可以由多个标记组成，可以是关键字，标识符，常量，字符串，符号。
**/
打印换行("世界你好");

/** 标识符
* 第一个字符必须是字母表中字母或下划线 _ 。
* 标识符的其他的部分由字母、数字和下划线组成。
* 标识符对大小写敏感。
* 可以用中文作为变量名，但不能是关键字
**/

/** 行分隔符
* 在 HorScript 中，一行代表一个语句结束。每个语句不需要像其它语言一样以分号 ; 结尾。
* 因为这些工作都会被自动完成。
**/
赋值 世界你好1 = "世界你好1"打印换行(世界你好1)
打印换行(世界你好1);打印换行(世界你好1);